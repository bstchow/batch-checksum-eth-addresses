# Batch Checksum addresses
Enter in any Ethereum addresses into the `input.json` file.

Then run 
```
npm start
```
in the terminal.

This should output all of the input.json addresses as checksummed Ethereum addresses.